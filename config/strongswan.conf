# StrongSwan daemon configuration
# strongswan.d/*.conf (e.g. charon-logging) included at top level so it merges into charon

charon {
    threads = 16
    load_modular = yes
}

include strongswan.d/*.conf
